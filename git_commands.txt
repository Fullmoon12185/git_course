# The first command to initialize git
git init
# The second command to tell git keep track my file
git add filename

# 3rd command to save the file
git commit -m "message"

# 4th command to checkout the file
git checkout commit_number

# reverse to the latest version
git checkout master

# see all commits
git log [--oneline]

I want to update some info from github
